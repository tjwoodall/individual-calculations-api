openapi: "3.0.3"

info:
  version: "7.0"
  title: Individual Calculations (MTD)
  description: |
    This version of the API is retired

components:
  securitySchemes:
    User-Restricted:
      type: oauth2
      description: HMRC supports OAuth 2.0 for authenticating [User-restricted](https://developer.service.hmrc.gov.uk/api-documentation/docs/authorisation/user-restricted-endpoints) API requests
      flows:
        authorizationCode:
          authorizationUrl: https://api.service.hmrc.gov.uk/oauth/authorize
          tokenUrl: https://api.service.hmrc.gov.uk/oauth/token
          refreshUrl: https://api.service.hmrc.gov.uk/oauth/refresh
          scopes:
            write:self-assessment: Grant write access
            read:self-assessment: Grant read access

servers:
  - url: https://test-api.service.hmrc.gov.uk
    description: Sandbox
  - url: https://api.service.hmrc.gov.uk
    description: Production

paths:
  /individuals/calculations/retired:
    get:
      summary: Retired
      description: |
        This version is retired and should not be used

      security:
        - User-Restricted:
            - read:self-assessment

      responses:
        410:
          description: Gone
